<h1>Add an image to the gallery</h1>
<%= simple_form_for @gallery, html: { multipart: true } do |f| %>
  <%= f.input :title %>
  <%= f.input :caption %>
  <%= f.input :image, as: :file %>

  <%= f.button :submit %>
<% end %>